# AGENTS.md

## Cursor Cloud specific instructions

This repository (`golfmeadows`) is currently an empty project with only an MIT LICENSE file. There is no application code, no dependencies, and no build system yet.

### Current state
- **No source code, frameworks, or dependencies** — the repo contains only a `LICENSE` file.
- **No lint, test, or build commands** are available.
- **No services** need to be started.

### When code is added
Once the project has source code and a build system, update this section with:
- How to install dependencies
- How to run lint, tests, and the dev server
- Any non-obvious caveats discovered during setup
